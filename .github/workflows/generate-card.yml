name: Generate animated ascii profile card

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - 'assets/profile.svg'
      - 'assets/profile_v2.svg'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install pillow requests numpy

      - name: Generate animated ASCII profile card (Kali Theme)
        env:
          GITHUB_LOGIN: Chahethsen12
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OUT_PATH: assets/profile.svg
        run: python scripts/generate_card.py

      - name: Generate animated ASCII profile card (Transparent Theme)
        env:
          GITHUB_LOGIN: Chahethsen12
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OUT_PATH: assets/profile_v2.svg
        run: python scripts/generate_card_v2.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/profile.svg assets/profile_v2.svg
          git diff --staged --quiet || git commit -m "chore: refresh ascii profile card"
          git push